# docs

このディレクトリは、ポケモンさんすうアプリの仕様・設計・運用ルールを管理する場所です。

## 現在のドキュメント

| ファイル | 役割 |
| --- | --- |
| `system_definition_v1.5.md` | V1.5 本番版の仕様定義書。ルート `index.html` の仕様参照元です。 |
| `system_definition_v2.md` | V2 の構想・要求仕様・データ構造・ロードマップを整理した仕様定義書です。 |
| `current_inventory.md` | 現在のリポジトリ構成、既存HTMLファイルの位置づけ、V1/V2/Firestore の関係を棚卸しした文書です。 |

## 現在の主要ファイルの位置づけ

| パス | 位置づけ |
| --- | --- |
| `index.html` | V1.5 本番版。明示的な Issue と承認がない限り変更しません。 |
| `canary/index_ui.html` | V2 UI 評価用プロトタイプ。参考用です。 |
| `canary/index_dev_masterdata_2.html` | マスターデータ取得・Firestore投入用の開発ダッシュボード。マスターデータ取得完了後は参考用です。 |

## 今後追加・整備する候補

| ファイル | 目的 |
| --- | --- |
| `data_model_v1_v2.md` | V1 `users`、V2 `users_v2`、マスター `masters/gen_{1..9}` のデータモデルを定義します。 |
| `migration_v1_to_v2.md` | V1 から V2 へのデータ移行仕様、再実行時の挙動、上書き可否、失敗時処理を定義します。 |
| `ui_ux_v2.md` | 子ども向け文言、保存状態表示、世代タブ、世代クリア UX を定義します。 |
| `release_and_canary.md` | V1 本番版と V2 開発版の分離、canary 配下の使い方、リリース方針を定義します。 |
| `review_checklist.md` | PR レビュー時の確認観点を整理します。 |

## ドキュメント更新タイミング

以下の場合は、コード変更と同時に関連ドキュメントを更新してください。

- Firestore の読み書き先を変更する場合
- V1/V2 のデータ構造を変更する場合
- 画面遷移、主要UI、子ども向け文言を変更する場合
- `canary/` 配下のファイルの役割を変更する場合
- マスターデータの取得・再構築・利用方針を変更する場合
- V1 から V2 への移行仕様を変更する場合

## 注意

`canary/` 配下の既存HTMLファイルはすべて参考扱いです。V2 本実装・開発用HTMLは、既存HTMLを正本化せず、新規ファイルとして作成します。
